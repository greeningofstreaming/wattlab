# WattLab Python dependencies
# Generated 2026-04-10 from GoS1 environment
#
# Install with: pip install -r requirements.txt
#
# NOTE: torch must be installed separately with the ROCm index for AMD GPU support:
#   pip install torch==2.5.1+rocm6.2 --index-url https://download.pytorch.org/whl/rocm6.2
# For CPU-only or NVIDIA, replace with the appropriate torch build from pytorch.org.
#
# System packages also required (apt):
#   sudo apt install lm-sensors ffmpeg

# Web framework
fastapi==0.135.3
uvicorn==0.42.0
python-multipart==0.0.22

# Power measurement (Tapo P110)
tapo==0.8.12

# Config
python-dotenv==1.2.2

# AI — image generation
diffusers==0.37.1
accelerate==1.13.0
safetensors==0.7.0

# AI — LLM / RAG
transformers==5.5.0
sentence-transformers==5.3.0
chromadb==1.5.5
pypdf==6.9.2
tokenizers==0.22.2

# PyTorch (AMD ROCm — see note above)
torch==2.5.1+rocm6.2

# Supporting libraries
huggingface_hub==1.9.0
numpy==2.4.4
pillow==10.2.0
requests==2.31.0
httpx==0.28.1
anyio==4.13.0
filelock==3.25.2
fsspec==2026.2.0
onnxruntime==1.24.4
packaging==24.0
regex==2026.4.4
sympy==1.13.1
